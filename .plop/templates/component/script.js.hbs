import { $ } from '../../js/helpers/selectors';

let SELECTOR = '.{{kebabCase name}}';

class {{pascalCase name}} {
  constructor() {
    if (! $(SELECTOR)) {
      return false;
    }

    //Your code below here
  }
}

export default {{pascalCase name}};
