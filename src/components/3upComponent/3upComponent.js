import { $ } from '../../js/helpers/selectors';

let SELECTOR = '.three-up-component';

class ThreeUpComponent {
  constructor() {
    if (! $(SELECTOR)) {
      return false;
    }

    //Your code below here
  }
}

export default ThreeUpComponent;
